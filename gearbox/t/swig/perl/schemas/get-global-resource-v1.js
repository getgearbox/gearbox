{
    "type": "object",
    "additionalProperties": false,
    "properties": {
        "this": {
            "type": "string",
            "enum": ["that"],
            "optional": true,
            "default": "that"
        }
    }
}